% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readVDem.R
\name{readVDem}
\alias{readVDem}
\title{Read V-Dem Governance Indicators}
\usage{
readVDem(subtype = "all")
}
\arguments{
\item{subtype}{character; \code{"all"} (default) for the four accountability indicators,
\code{"stateCapacity"} for five state-capacity indicators that replace WGI Government
Effectiveness (see Details), or any V-Dem column code (e.g. \code{"v2x_corr"}).}
}
\value{
A \code{\link[magclass:magclass]{magpie}} object with dimensions
\verb{[iso3c, year, indicator]}. Variable names follow the
\code{"<Concept> (VDem)"} convention (see ADR 0002).
}
\description{
Default set (\code{subtype = "all"}) returns four indicators:
\itemize{
\item Rule of Law (VDem) — v2x_rule
\item Vertical Accountability (VDem) — v2x_veracc
\item Horizontal Accountability (VDem) — v2x_horacc
\item Diagonal Accountability (VDem) — v2x_diagacc
}
All V-Dem indicators are on a 0–1 scale (higher = better governance).
Any other V-Dem column can be requested by passing its code as \code{subtype}
(e.g. \code{"v2x_corr"}). Use \code{\link[=listVDemIndicators]{listVDemIndicators()}} to see what columns are
available in the installed dataset file.
}
\details{
Reads governance indicators from the Varieties of Democracy (V-Dem)
Country-Year dataset CSV file.

Place the V-Dem Country-Year CSV (e.g. \code{V-Dem-CY-Core-v14_0.csv}) in the
\code{VDem} madrat source folder. Download from
\url{https://www.v-dem.net/data/the-v-dem-dataset/} or generate with
\code{write.csv(vdemdata::vdem, "vdem.csv")} from the
\href{https://github.com/vdeminstitute/vdemdata}{vdemdata} R package.

\code{country_text_id} values are treated as Gleditsch-Ward character codes and
converted to ISO 3166-1 alpha-3 via the \code{countrycode} package. Rows that do
not map (historical states, aggregates) are silently dropped.
}
\author{
Renato Rodrigues
}

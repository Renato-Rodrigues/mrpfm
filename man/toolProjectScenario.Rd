% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolProjectScenario.R
\name{toolProjectScenario}
\alias{toolProjectScenario}
\title{toolProjectScenario}
\usage{
toolProjectScenario(
  x,
  y,
  mode = "global_percentile",
  percentile = 75,
  fixedValue = NULL,
  convergenceYear = 2100,
  shape = "linear",
  midpointYear = NULL,
  keepIfAboveTarget = TRUE
)
}
\arguments{
\item{x}{magpie object (one or more variables)}

\item{y}{integer vector of target years to output}

\item{mode}{projection mode: \code{"constant"} (forward-fill last value), \code{"global_max"},
\code{"global_percentile"} (default), \code{"global_mean"}, or \code{"fixed_value"}. Target is
computed cross-sectionally from all regions at the last non-NA year in \code{x}.}

\item{percentile}{numeric 0-100, percentile target; used when \code{mode = "global_percentile"}.
Default 75.}

\item{fixedValue}{numeric target value; used when \code{mode = "fixed_value"}.}

\item{convergenceYear}{integer year by which convergence is complete. Default 2100.}

\item{shape}{convergence curve shape: \code{"linear"} (default) or \code{"logistic"}.}

\item{midpointYear}{integer year at which 50\% of the gap is closed; logistic only.
Defaults to the midpoint between the anchor year (last data year) and \code{convergenceYear}.}

\item{keepIfAboveTarget}{logical. If \code{TRUE} (default), regions whose value at the anchor
year already exceeds the target are forward-filled at that value rather than pulled down.}
}
\value{
magpie object with the same regions and variables as \code{x}, spanning years \code{y}.
}
\description{
Projects a magpie object forward in time with configurable convergence modes.
Intended for institutional quality and control variables where no scenario-specific projections
exist. Supports constant projection, or convergence toward a cross-sectional target
(global percentile, mean, max, or fixed value) with linear or logistic convergence shapes.
}
\author{
Renato Rodrigues
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readOECDValueAdded.R
\name{readOECDValueAdded}
\alias{readOECDValueAdded}
\title{Read OECD value added by economic activity}
\usage{
readOECDValueAdded()
}
\value{
A \code{\link[magclass:magclass]{magpie}} object \verb{[iso3c, year, activity]} of
value added (current prices, national currency or the source unit — only the
within-country cross-activity SHARES are consumed downstream, so the unit
cancels).
}
\description{
Reads the CSV downloaded by \code{\link[=downloadOECDValueAdded]{downloadOECDValueAdded()}} into a magpie object
\verb{[iso3c, year, activity]}. Rows are filtered to gross value added (transaction
\code{B1G}) and, where a price-base column exists, to current prices (\code{V}) so the
resulting sector shares are shares of nominal value added. Activity variables
are named \code{"<code> <label>"} (dots/commas stripped — dots are magclass
subdimension separators), so \code{\link[=computeSectorWeights]{computeSectorWeights()}} can match either the
ISIC code (\code{D35}, \code{C}, \code{L}, \code{H}) or the label text (electricity / manufactur /
real estate / transport).
}
\author{
Renato Rodrigues
}

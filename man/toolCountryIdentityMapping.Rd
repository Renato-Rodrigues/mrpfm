% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolCountryIdentityMapping.R
\name{toolCountryIdentityMapping}
\alias{toolCountryIdentityMapping}
\title{toolCountryIdentityMapping}
\usage{
toolCountryIdentityMapping(fileName = "regionmapping_country.csv")
}
\arguments{
\item{fileName}{character; name of the generated mapping file inside the
regional mapping folder.}
}
\value{
The mapping file name (character), resolvable by
\code{\link[madrat:toolGetMapping]{madrat::toolGetMapping()}} with \code{type = "regional"} and
\code{where = "mappingfolder"}.
}
\description{
Generates (once) and returns the country-identity region mapping,
so a country-resolution run needs no hand-made mapping file. The mapping maps
every country to itself (\code{RegionCode == CountryCode}), which makes
\code{aggregate = TRUE} a per-country no-op: the whole regional pipeline runs at
country resolution unchanged. The file is derived from the shipped
\code{regionmappingH12.csv} (mapping folder if present, else the copy bundled with
madrat) and written to \verb{<mappingfolder>/regional/regionmapping_country.csv}
on first use; subsequent calls return the existing file untouched.

Callers that pass the returned name to \code{calcOutput(regionmapping = )} must
remember that \code{calcPolicyStringency()}'s coverage filter reads the GLOBAL
\code{madrat::getConfig("regionmapping")} — the pfm panel builders scope the global
config around their data calls when given the \code{"country"} sentinel (see
\code{pfm::resolveRegionMapping}).
}
\author{
Renato Rodrigues
}

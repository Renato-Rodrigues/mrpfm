% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcPolicyRatchetEvents.R
\name{calcPolicyRatchetEvents}
\alias{calcPolicyRatchetEvents}
\title{Calculate country-level climate-policy ratchet-up events from CAPMF instruments}
\usage{
calcPolicyRatchetEvents(
  source = "official",
  level = "LEV3",
  jumpThreshold = 0.5
)
}
\arguments{
\item{source}{character; \code{"official"} (default) or \code{"expanded"} (simulated
stub — sensitivity only, see \code{\link[=calcPolicyStringency]{calcPolicyStringency()}}).}

\item{level}{character; CAPMF code level to read events from. Default
\code{"LEV3"} (instruments); \code{"LEV4"} if present in the source.}

\item{jumpThreshold}{numeric; minimum stringency increase (index points) of a
single instrument to count as a tightening event. Default \code{0.5}.}
}
\value{
A list with:
\describe{
\item{x}{\code{\link[magclass:magclass]{magpie}} \verb{[iso3c, year, variable]} with
\verb{Ratchet Event} (0/1: any event), \verb{Ratchet Count} (number of instruments
with an event) and \verb{Ratchet Intensity} (summed positive stringency jumps
over event instruments).}
\item{weight}{\code{NULL} — regional aggregation sums counts/intensities (the
0/1 event column then counts event-countries per region); the intended
use is country-level (\code{aggregate = FALSE}).}
\item{unit}{\code{"events (0/1); count; index points"}}
\item{description}{character description}
}
}
\description{
Converts the instrument-level CAPMF stringency paths (LEV3 codes; ~50+
instruments per country-year) into discrete \strong{ratchet-up events} — the
outcome of the event-history reading of policy feasibility (Tier-1 direction 2
in \code{docs/psm-theoretical-directions.md}): a country-year has an event when any
instrument is \emph{adopted} (stringency moves off zero) or \emph{tightened}
by at least \code{jumpThreshold} index points. Unlike the smoothed
\code{\link{calcPolicyStringency}} index, events are country-level acts of
identifiable polities and carry far more identifying variation.
}
\details{
Never imputed: a country-year needs a finite instrument value in both t-1 and
t to be at risk; years with no observable instrument pair are \code{NA} (not
zero). The first panel year is \code{NA} by construction.
}
\author{
Renato Rodrigues
}

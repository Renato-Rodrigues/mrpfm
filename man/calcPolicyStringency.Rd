% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcPolicyStringency.R
\name{calcPolicyStringency}
\alias{calcPolicyStringency}
\title{Calculate CAPMF-based Policy Stringency outcomes (PSM dependent variables)}
\usage{
calcPolicyStringency(
  source = "official",
  minCoverage = 0.8,
  sectorResolution = "two",
  weighting = "equal"
)
}
\arguments{
\item{source}{character; \code{"official"} (default) uses the published CAPMF database.
\code{"expanded"} uses the extended file — \strong{currently a simulated stub written by
\code{\link[=downloadCAPMF]{downloadCAPMF()}} (mock values for PRY/AGO/EGY/PAK)}: sensitivity use only,
never a headline fit.}

\item{minCoverage}{numeric, logical or NULL; threshold of GDP and Population
coverage below which a whole region's countries are excluded (set \code{NA}).
Defaults to 0.8. Set to \code{FALSE} or \code{NULL} to disable.}

\item{sectorResolution}{character; \code{"two"} (default) returns the Bulk/Diffuse/
composite outcomes, \code{"four"} the raw Electricity/Industry/Buildings/Transport
sector indices.}

\item{weighting}{how the sector indices are aggregated into Bulk and Diffuse
(two-sector only; ignored for \code{sectorResolution = "four"}). \code{"equal"} (default)
uses the OECD simple mean; \code{"ghg"} or \code{"gdp"} use GHG-/GDP-share weights à la
Metta-Versmessen (2025), sourced in the data layer by \code{\link[=computeSectorWeights]{computeSectorWeights()}}.
You may also pass weights directly as a \strong{named numeric vector} over the sector
short names (\code{c(elec=, ind=, buildings=, transport=)}, broadcast to all cells) or
a \strong{magpie} \verb{[iso3c, year, sector]} of per-cell weights. Weights are renormalised
within the non-missing members of each pairing.}
}
\value{
A list with:
\describe{
\item{x}{\code{\link[magclass:magclass]{magpie}} object \verb{[iso3c, year, variable]} with
variables \code{bulk}, \code{diffuse} and (if resolvable) \code{composite}. NOTE: at country
level, missing observations are encoded as value 0 with weight 0 (an
aggregation device) — use \code{weight > 0} to identify real data, or
\code{readSource("CAPMF")} for raw values.}
\item{weight}{SSP2 population weights, zeroed where the outcome is missing.}
\item{aggregationArguments}{\code{zeroWeight = "setNA"} so no-data regions stay \code{NA}.}
\item{unit}{\code{"index (0-10)"}}
\item{description}{character description}
}
}
\description{
Aggregates the OECD CAPMF sector-level stringency indices into the outcome
variables of the Policy Stringency Model (ADR 0036): \strong{bulk} (mean of the
Electricity and Industry sector indices), \strong{diffuse} (mean of the Buildings
and Transport sector indices) and, where the module-level indices are present,
\strong{composite} (mean of the Sectoral / Cross-sectoral / International LEV1
module indices). Each sector index is itself reconstructed from the deployed
CAPMF LEV2 codes, which split every sector into market-based (\verb{LEV2_SEC_*_MBI})
and non-market-based (\verb{LEV2_SEC_*_NMBI}) instruments: a sector's stringency is the
mean of its instrument-type children, and Bulk/Diffuse then average the relevant
sector indices. Sector aggregation uses the OECD's own convention of simple
averages over the available children.
}
\details{
Unlike \code{\link[=calcCAPMF]{calcCAPMF()}}, values are \strong{never imputed}: these series are model
\emph{outcomes}, and fabricated outcome values would contaminate the estimation.
Missing data is instead handled at aggregation time — the weight is set to zero
where the outcome is missing, so a region's value is the population-weighted mean
over its \emph{data-bearing} member countries only, and a region with no data at
all aggregates to \code{NA} (\code{zeroWeight = "setNA"}), never to a fabricated zero.
}
\author{
Renato Rodrigues
}

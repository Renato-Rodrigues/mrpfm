% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolNormalize.R
\name{toolNormalize}
\alias{toolNormalize}
\title{Normalize magpie object}
\usage{
toolNormalize(
  data,
  method = "min-max",
  scope = "global",
  minVal = NULL,
  maxVal = NULL,
  symmetric = FALSE,
  targetRange = NULL,
  clamp = FALSE
)
}
\arguments{
\item{data}{A magpie object to be normalized.}

\item{method}{Normalization method. Currently only "min-max" is supported.}

\item{scope}{Normalization scope: "global" (across all regions and years),
"region" (per region across years), or "time" (per year across regions).}

\item{minVal}{Optional exogenous minimum value(s). Can be a single numeric,
a named vector/list by variable name, or NULL (calculated from data).}

\item{maxVal}{Optional exogenous maximum value(s). Can be a single numeric,
a named vector/list by variable name, or NULL (calculated from data).}

\item{symmetric}{Boolean. If TRUE, the range is made symmetric around zero
by using the maximum absolute value of the bounds. Defaults to targetRange \verb{[-1, 1]}.}

\item{targetRange}{Optional target range for normalization (e.g., c(0, 1) or c(-1, 1)).
If NULL, it defaults to \verb{[0, 1]} if all values are non-negative, and \verb{[-1, 1]} otherwise.}

\item{clamp}{Logical. If TRUE, output values are clamped to \code{targetRange} after scaling.
Useful when exogenous bounds are derived from a reference period and applied to data that
may exceed that range (e.g., historical bounds applied to future projections).
Defaults to FALSE.}
}
\value{
A normalized magpie object.
}
\description{
Normalizes a magpie object using min-max scaling (x - min / (max - min)).
Normalization is performed per variable (dimension 3).
}
\author{
Renato Rodrigues
}
